# Open old `R_SEQUOIA` vector file

Open old `R_SEQUOIA` vector file

## Usage

``` r
seq1_read(dirname = ".", layer)
```

## Arguments

- dirname:

  `character` Directory where the file is located. Defaults to the
  current working directory.

- layer:

  `character` Layer name.

## Value

An `sf` object.
