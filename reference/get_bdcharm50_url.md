# List BDCharm50 archive URLs

Scrapes the BDCharm50 download directory and returns available ZIP
archive URLs, named by archive filename.

## Usage

``` r
get_bdcharm50_url()
```

## Value

A named `character` vector. Names are ZIP filenames and values are
download URLs.
