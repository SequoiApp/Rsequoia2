# Rename GeoPackage identifier

Update the Sequoia identifier field in one GeoPackage.

## Usage

``` r
seq_gpkg_id_rename(path, new_id)
```

## Arguments

- path:

  Character. Path to a GeoPackage.

- new_id:

  Character. New identifier value.

## Value

A list with `file`, `status`, and `message`.
