# Right-pad a string with zeros

Internal helper used to format fixed-width codes.

## Usage

``` r
pad_left(x, width, fill = "0")
```

## Arguments

- x:

  `character` String to pad

- width:

  `integer` Desired total width

- fill:

  `character` Filler to pad with

## Value

A zero-padded character string.
