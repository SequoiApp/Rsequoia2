# Build IDU

Internal helper used to build idu

## Usage

``` r
idu_build(dep, com, prefix, section, numero)
```

## Arguments

- dep:

  `character` dep code

- com:

  `character` com code

- prefix:

  `character` prefic code

- section:

  `character` section code

- numero:

  `character` numero code
