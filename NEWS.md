# Rsequoia2 (development version)

## v0.0.1

- Remove `download_brgm` in favor of `download_bdcharm50` & `download_carhab`
- Add cache config in `inst/config/seq_caches.yaml` and associated helpers 
function in `config-cache.R`

