% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config-layers.R
\name{get_path}
\alias{get_path}
\title{Construct filepath for a given layer key}
\usage{
get_path(key, verbose = FALSE)
}
\arguments{
\item{key}{\code{character} Name of a layer key to match against the entries
defined in \code{inst/config/seq_layers.yaml}. (see \emph{Details} for partial matching).}

\item{verbose}{\code{logical} If \code{TRUE}, display messages.}
}
\value{
Invisibly returns a single forest ID (character scalar).
}
\description{
Builds the expected output filename for a given layer key by combining:
(1) the forest ID extracted from the local *_matrice.xlsx file, and
(2) the corresponding layer definition stored in \code{inst/config/seq_layers.yaml}.
}
\details{
The function resolves the input \code{key} using \strong{partial matching} against
keys defined in \code{inst/config/seq_layers.yaml}.

If exactly \strong{one} entry matches, it is selected : for example,
\code{key = "znieff1"} can be used to match \code{v.mnhn.znieff1.poly} because only
one key contain \verb{"znieff1}

If \strong{multiple} entries match, the user is shown the ambiguous options and
must provide a more specific key.

Note: \strong{\code{seq_layers.yaml} is part of the package and should not be modified.}
}
