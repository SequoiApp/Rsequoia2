% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elevation.R
\name{seq_elevation}
\alias{seq_elevation}
\title{Download and Compute Elevation Products for a Sequoia Project}
\usage{
seq_elevation(
  dirname = ".",
  buffer = 200,
  res = 1,
  crs = 2154,
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{dirname}{\code{character} Directory where the matrice file is located.
Defaults to the current working directory.}

\item{buffer}{\code{numeric}; Buffer around \code{x} (in \strong{meters}) used to enlarge
the download area.}

\item{res}{\code{numeric}; resolution specified in the units of the coordinate
system (see \code{\link[happign:get_wms_raster]{happign::get_wms_raster()}})}

\item{crs}{\code{numeric} or \code{character}; CRS of the returned raster (see
\code{\link[happign:get_wms_raster]{happign::get_wms_raster()}})}

\item{overwrite}{\code{logical} If \code{TRUE}, file is overwritten.}

\item{verbose}{\code{logical}; If \code{TRUE}, display messages.}
}
\value{
A named list of file paths written by \code{\link[=seq_write]{seq_write()}}, one per \code{type}.
}
\description{
Downloads elevation datasets (DEM, DSM) from the IGN WMS, computes derived
products (CHM, slope, aspect), and writes all resulting rasters into the
Sequoia project directory.
}
\details{
This function is a high-level convenience wrapper around:
\itemize{
\item \code{\link[=get_dem]{get_dem()}} — Digital Elevation Model
\item \code{\link[=get_dsm]{get_dsm()}} — Digital Surface Model
\item \code{\link[=get_chm]{get_chm()}} — Canopy Height Model
\item \code{\link[=get_slope]{get_slope()}} — Slope raster
\item \code{\link[=get_aspect]{get_aspect()}} — Aspect raster
}

It automatically reads the project's \code{parca} layer, downloads the necessary
elevation data for its extent (with an optional buffer), computes all derived
products, and saves them using \code{\link[=seq_write]{seq_write()}}.
}
\seealso{
\code{\link[=get_dem]{get_dem()}}, \code{\link[=get_dsm]{get_dsm()}}, \code{\link[=get_chm]{get_chm()}}, \code{\link[=get_slope]{get_slope()}}, \code{\link[=get_aspect]{get_aspect()}}
}
