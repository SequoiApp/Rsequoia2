% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdf-regexp.R
\name{parse_rp}
\alias{parse_rp}
\title{Parse a French cadastral property record (RP) PDF}
\usage{
parse_rp(pdf)
}
\arguments{
\item{pdf}{Path to a cadastral RP PDF file.}
}
\value{
A data frame with one row per parcel (main and detail).
Columns include parcel identifiers, land use, surface (in hectares),
and administrative codes.
}
\description{
\code{parse_rp()} extracts and parses non-built parcels from a French cadastral
property record PDF (Releve de Propriete - RP).
}
\details{
The function reads the PDF, identifies main parcels and detail rows,
computes parcel surfaces, and returns a tidy data frame ready for analysis.

Only the \emph{non-built} section of the RP is parsed.
Image-only (scanned) PDFs are not supported.
}
\examples{
\dontrun{
df <- parse_rp("releve_propriete.pdf")
head(df)
}

}
