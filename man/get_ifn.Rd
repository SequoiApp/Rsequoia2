% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ifn.R
\name{get_ifn}
\alias{get_ifn}
\title{Retrieve IFN regional layers by intersecting an area}
\usage{
get_ifn(x, type = c("ser", "ser_ar", "rfn", "rfd", "zp"), cache = NULL)
}
\arguments{
\item{x}{An \code{sf} object defining the input area of interest.}

\item{type}{A character string specifying the IFN regional dataset to use.
Must be one of:
\itemize{
\item \code{"ser"}: sylvo-écorégions
\item \code{"ser_ar"}: sylvo-écorégions (aggregated regions)
\item \code{"rfn"}: réseau forestier national
\item \code{"rfd"}: réseau forestier départemental
\item \code{"zp"}: zones de production
}}

\item{cache}{A character string defining the cache directory.
Defaults to a package-specific cache directory created with
\code{tools::R_user_dir("Rsequoia2", which = "cache")}.}
}
\value{
An \code{sf} object containing the region features intersecting \code{x}.
Returns \code{NULL} if no region intersects the input geometry.
}
\description{
Downloads (if necessary), caches and extracts IFN regional layers
intersecting a given area of interest.
}
\details{
The function retrieves official IGN regional datasets provided as
shapefiles in Lambert-93 projection. Downloaded archives are stored
locally and reused on subsequent calls.

The regional layer is automatically reprojected to match the
coordinate reference system of \code{x} before computing spatial
intersections.

Only features intersecting the input geometry are returned. No
geometry modification (e.g. clipping) is applied.
}
